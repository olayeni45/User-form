const modalTypes = {
  empty: "empty",
  negative: "negative",
};

const modalMessages = {
  empty: " Please enter a valid name and age (non-empty values).",
  negative: "Please enter a valid age (>0). ",
};

export { modalMessages, modalTypes };
